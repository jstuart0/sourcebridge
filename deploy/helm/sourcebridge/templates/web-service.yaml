apiVersion: v1
kind: Service
metadata:
  name: {{ include "sourcebridge.fullname" . }}-web
  labels:
    {{- include "sourcebridge.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sourcebridge.web.selectorLabels" . | nindent 4 }}
