# homebrew-tap

Homebrew tap for [SourceBridge](https://sourcebridge.ai).

## Install

```bash
brew install sourcebridge-ai/tap/sourcebridge
```

## Upgrade

```bash
brew upgrade sourcebridge
```

The formula is updated automatically by the SourceBridge release workflow on every tagged release.
